---
interactive:
  version: 1
  enabled: true
  lessonId: "macro-basics-05-test"
  title: "Тест: Итоги 1 модуля. Базовые макросы"
  draft: false
  test:
    timeLimit: 300
    passingScore: 80
    attempts: 3
    questions:
      - id: "q1_api"
        text: "Какой объект является главным (точкой входа) для взаимодействия с документом и макросах Р7-Офис?"
        options:
          - id: "a"
            text: "Application"
          - id: "b"
            text: "Api"
          - id: "c"
            text: "DocumentBuilder"
          - id: "d"
            text: "ActiveSheet"
        correctOption: "b"
        explanation: "В Р7-Офис все обращения начинаются с глобального объекта Api. Он дает доступ к активному листу, поиску и другим элементам."

      - id: "q2_methods"
        text: "Какова правильная последовательность вызовов (цепочка), чтобы получить активный лист, а затем ячейку A1?"
        options:
          - id: "a"
            text: "Api.GetActiveSheet().GetRange('A1')"
          - id: "b"
            text: "Api.Range('A1').GetActiveSheet()"
          - id: "c"
            text: "Api.GetSheet().A1"
          - id: "d"
            text: "Worksheet.Api().GetRange('A1')"
        correctOption: "a"
        explanation: "Объектная иерархия требует строгого соблюдения вложенности: сначала мы у Api просим активный лист (GetActiveSheet()), а затем у листа просим конкретный диапазон (GetRange())."

      - id: "q3_architecture"
        text: "Зачем в эталонных макросах Р7-Офис используется комбинация 'try / catch' и 'use strict'?"
        options:
          - id: "a"
            text: "Для перехвата ошибок без 'зависания' редактора и защиты от неявного объявления переменных."
          - id: "b"
            text: "Только для ускорения работы вычислений."
          - id: "c"
            text: "Это требование для подключения внешних баз данных MySQL."
          - id: "d"
            text: "Без них невозможно поменять цвет ячейки."
        correctOption: "a"
        explanation: "Этот архитектурный эталон внедряется для надежности: strict-mode не дает опечататься в переменных, а try/catch ловит непредвиденные сбои (чтобы макрос не сломал весь файл пользователя) и красиво выводит ошибку."
      - id: "q4_syntax_error"
        text: "Какой из примеров ниже выдаст ошибку при выполнении макроса?"
        options:
          - id: "a"
            text: "let x = sheet.GetRange(\"A1\").GetValue();"
          - id: "b"
            text: "sheet.GetRange(\"B2\").SetValue(123);"
          - id: "c"
            text: "let sheet = Api.GetActiveSheet;"
          - id: "d"
            text: "console.log(\"Привет\");"
        correctOption: "c"
        explanation: "Ошибка кроется в варианте 'c': метод GetActiveSheet — это функция-действие, и он обязательно требует круглых скобок `()` при вызове. Правильно: Api.GetActiveSheet()."

      - id: "q5_math"
        text: "В ячейку записано текстовое значение '10' (строка). Макрос считывает это значение: let val = sheet.GetRange('A1').GetValue(); Что будет, если мы попытаемся сделать val + 5?"
        options:
          - id: "a"
            text: "Результат будет 15"
          - id: "b"
            text: "Результат будет '105' (конкатенация: строка + число)"
          - id: "c"
            text: "Макрос упадет с ошибкой"
          - id: "d"
            text: "Результат будет 0"
        correctOption: "b"
        explanation: "В JavaScript знак + означает не только сложение, но и 'склеивание' (конкатенацию) строк. Если хотя бы один элемент строковый ('10'), '10' + 5 склеятся в '105'. Решение: преобразовать строку в число, например Number(val) + 5."
---

# Итоговый тест Модуля 1

Поздравляем с прохождением первого модуля! 

В этом модуле мы заложили прочный фундамент: вы познакомились с переменными и синтаксисом JavaScript, разобрались в объектной модели Р7-Офис, научились "добывать" ячейки с помощью `Api` и `GetRange`, а также считывать и записывать в них данные. Вы даже написали свой первый макрос-калькулятор!

Пришло время закрепить знания. 

## 🏆 Правила тестирования:
- В тесте 5 вопросов.
- Время выполнения ограничено (5 минут).
- На вопросы нужно отвечать последовательно.
- Проходной балл — 80% (можно сделать только одну ошибку!). 
- Если вы ошибетесь, система выдаст подробное объяснение, чтобы вы поняли, в чем суть.

Удачи! Нажмите "Начать тест" ниже, чтобы запустить таймер.
