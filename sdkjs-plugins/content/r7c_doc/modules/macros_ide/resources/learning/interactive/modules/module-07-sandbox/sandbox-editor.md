---
title: "Редактор макросов"
type: practice
difficulty: free
task:
  goal: "Свободный редактор макросов. Пишите любой код и запускайте в R7 Office."
starterCode: |
  // Редактор макросов — пишите любой код

  (function () {

    var sheet = Api.GetActiveSheet();

    // Ваш код здесь...
    // Например:
    sheet.GetRange("A1").SetValue("Привет из макроса!");

  })();
checks: []
---

# Редактор макросов

Здесь нет заданий и проверок — только вы и редактор.

Пишите любой макрос и запускайте его на активном листе.

## Хотите мощный редактор с автодополнением?

> **r7 Code** — специализированный плагин для разработки макросов R7 Office.
>
> Возможности:
> - Полноценная среда с подсветкой синтаксиса и автодополнением
> - Встроенная документация по API прямо в редакторе
> - Быстрый запуск, история кода, несколько вкладок
> - Экспорт и импорт макросов
>
> SmartDocumentation — это учебная среда.
> Для серьёзной разработки установите **r7 Code**.
