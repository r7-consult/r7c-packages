# Урок 2: Знакомство с плагином для Excel

Введение: от теории к практике

После изучения теоретических основ табличных моделей и архитектуры данных, настало время применить полученные знания на практике. В этом уроке мы будем работать с плагином «Слайдер данные» для Microsoft Excel — мощным инструментом для выполнения DAX-формул и работы с табличными моделями. Основное преимущество этого подхода — возможность писать и тестировать DAX-формулы в привычной среде Excel, получая результаты прямо в таблицы.

Установка плагина «Слайдер данные»

Шаг 1: Загрузка и установка

Начнем с установки плагина. Скачайте установочный файл плагина «Слайдер данные» по ссылке, предоставленной в материалах курса. После загрузки запустите установщик и следуйте инструкциям мастера установки.

## Системные требования

Microsoft Excel 2010 или выше

.NET Framework 4.5 или более поздняя версия

Минимум 2 ГБ оперативной памяти для комфортной работы

После завершения установки запустите Excel. Вы должны увидеть новую вкладку «Слайдер данные» на ленте Excel. Если вкладка не появилась, активируйте надстройку через меню «Файл» → «Параметры» → «Надстройки» → «Управление: Надстройки COM» → «Перейти» и поставьте галочку напротив «Слайдер данные».

Шаг 2: Активация лицензии

При первом запуске плагин запросит лицензионный ключ. Введите ключ, предоставленный для курса, или активируйте пробную версию. Пробная версия предоставляет полный функционал на 60 дней, чего достаточно для изучения всего курса.

Настройка подключения к табличной модели

Использование Data Manager

После успешной установки перейдите на вкладку «Слайдер данные» и нажмите кнопку «Data Manager». Это центральная точка управления всеми подключениями и запросами. Data Manager открывается в отдельном окне и предоставляет удобный интерфейс для работы с подключениями.

Настройка подключения к серверу

## Для подключения к тестовой табличной модели вам надо будет сделать

Нажать на шестеренки и вставить этот айпи адрес: http://89.169.162.67:55080

Скачайте подключение с сайта… и импортируйте этот файл в Data Manager. Вы конечно можете ввести данные вручную, но пока вам это не нужно.

Создание первой DAX-формулы

Шаг 1: Создание нового запроса

В Data Manager нажмите кнопку «New Query». Откроется диалоговое окно создания запроса, где необходимо указать:

Имя запроса: Дайте понятное имя, например, “Общие продажи”

Выбор модели: В выпадающем списке выберите “Adventure Works” (или полное имя “Adventure Works DW”)

Язык запроса: Выберите DAX (также доступны MDX и SQL, но мы изучаем DAX)

Шаг 2: Написание DAX-формулы

## В открывшемся редакторе запросов введите вашу первую DAX-формулу

```dax
EVALUATE
SUMMARIZE(
FactInternetSales,
DimProduct[Category],
"Total Sales", SUM(FactInternetSales[SalesAmount])
)
```

## Разбор формулы

```dax
EVALUATE — начало DAX-запроса для возврата таблицы
```

SUMMARIZE — функция группировки данных

FactInternetSales — основная таблица фактов

DimProduct[Category] — столбец группировки из таблицы измерений

```dax
"Total Sales" — имя новой меры
SUM(FactInternetSales[SalesAmount]) — расчет суммы продаж
```

Шаг 3: Предварительный просмотр

Нажмите кнопку «Preview» для выполнения запроса и просмотра результатов. В окне предпросмотра вы увидите:

Таблицу с результатами (категории продуктов и суммы продаж)

Время выполнения запроса

Количество возвращенных строк

Если результаты соответствуют ожиданиям, нажмите «Save» для сохранения запроса. Запрос сохранится в библиотеке запросов и будет доступен для последующего использования.

Выгрузка результатов в Excel

Создание отчета из запроса

## После сохранения запроса переходим к созданию отчета в Excel

В Data Manager нажмите кнопку «New Report»

Имя отчета: Введите понятное имя, например, “Анализ продаж”

Выбор запроса: В выпадающем списке “Query” выберите ранее сохраненный запрос

## Целевой лист: Укажите, на какой лист Excel выгружать данные

Можно выбрать существующий лист

Создать новый лист с указанным именем

Указать конкретную ячейку начала вывода (например, A1)

## Обывает визуальный браузер модели. Здесь вы можете

Просматривать связи между таблицами

Видеть доступные меры и их группы

Изучать атрибуты и свойства столбцов

Однако, как правильно отмечено, для практической работы удобнее использовать DAX-формулы, так как они:

Точно определяют нужные данные

Легко сохраняются и переиспользуются

Могут быть автоматизированы

Предоставляют больше возможностей для сложного анализа

Советы по эффективной работе

Организация запросов

Используйте понятные имена: Называйте запросы описательно, например, “YTD_Sales_By_Region_2013”

Группируйте по темам: Создавайте папки в Data Manager для разных типов анализа

## Документируйте запросы: Добавляйте комментарии в DAX-код

```dax
// Анализ продаж по регионам за текущий год
EVALUATE ...
```

Оптимизация производительности

Используйте фильтры: Ограничивайте объем данных на уровне запроса

Избегайте декартова произведения больших таблиц: Это может привести к огромным результатам

Применяйте фильтрацию пустых значений: Исключайте пустые строки и столбцы

Отладка запросов

## Если запрос не работает

Проверьте правильность имен таблиц и столбцов

Убедитесь в наличии квадратных скобок для имен с пробелами

Используйте Preview для пошаговой проверки

Начинайте с простого запроса и постепенно усложняйте

Практические задания

Задание 1: Базовая формула

Создайте DAX-формулу, которая показывает общую сумму продаж через интернет.

Задание 2: Множественные меры

Напишите формулу, отображающую три меры (общие продажи, количество клиентов, количество заказов) в одной таблице.

Задание 3: Группировка данных

Создайте формулу для анализа продаж по категориям продуктов.

Типичные проблемы и их решения

Проблема: “Не удается подключиться к серверу”

## Решение

Проверьте правильность IP-адреса

Убедитесь, что сервер доступен (ping)

Проверьте настройки файрвола

Используйте готовый файл подключения

Проблема: “Запрос возвращает пустой результат”

## Решение

Проверьте правильность имен столбцов

Убедитесь, что данные существуют для выбранного периода

Уберите излишние фильтры

Проблема: “Слишком долгое выполнение запроса”

## Решение

Добавьте фильтры для ограничения данных

Используйте конкретные значения столбцов вместо всех значений

Оптимизируйте формулы DAX

Заключение

В этом уроке мы освоили практическую работу с DAX через плагин «Слайдер данные». Вы научились:

Устанавливать и настраивать плагин

Подключаться к табличной модели через Data Manager

Создавать и сохранять DAX-формулы

Выгружать результаты в Excel для дальнейшего анализа

Этот практический опыт станет основой для всех последующих уроков. В следующем модуле мы углубимся в синтаксис DAX и научимся писать более сложные формулы. Рекомендуется попрактиковаться с различными формулами, чтобы освоиться с интерфейсом плагина перед переходом к продвинутым темам.
