---
interactive:
  version: 1
  enabled: true
  lessonId: "macro-ranges-18-test"
  title: "Тест: Итоги 3 модуля. Мастерство диапазонов"
  draft: false
  test:
    timeLimit: 420
    passingScore: 80
    attempts: 3
    questions:
      - id: "q1_loop"
        text: "Почему при физическом удалении строк из таблицы (используя метод Delete) программисты всегда применяют 'обратный цикл' (reverse loop: от 10 к 1), а не прямой (от 1 к 10)?"
        options:
          - id: "a"
            text: "Обратный цикл выполняется процессором быстрее."
          - id: "b"
            text: "Прямой цикл не умеет удалять больше одной строки за раз."
          - id: "c"
            text: "При прямом цикле удаление вызывает сдвиг индексов строк вверх, из-за чего цикл пропускает (не проверяет) каждую следующую строку после удаленной."
          - id: "d"
            text: "API Р7-Офис выдаст ошибку SyntaxError при попытке использовать инкремент (i++) вместе с методом Delete."
        correctOption: "c"
        explanation: "Удаление 'сдвигает' оставшиеся данные. Если идти сверху вниз, следующая строка 'упадет' на место только что удаленной, а цикл пойдет дальше, пропустив её. Обратный цикл идет снизу вверх, поэтому сдвиги ниже проверенной зоны не ломают индексацию."

      - id: "q2_clear"
        text: "Какой метод используется, если нам нужно получить идеально пустые ячейки (обнулить данные), но при этом мы НЕ хотим сдвигать соседние колонки или нарушать структуру таблицы (например, в сложном шаблоне отчета)?"
        options:
          - id: "a"
            text: "range.Delete('up')"
          - id: "b"
            text: "range.SetValue(' ')"
          - id: "c"
            text: "range.Clear()"
          - id: "d"
            text: "range.SetHidden(true)"
        correctOption: "c"
        explanation: "Метод Clear() обнуляет содержимое (содержимое удаляется), но физически сама сетка ячеек не схлопывается (в отличие от Delete). Запись ' ' (пробела) оставляет невидимый 'мусор', ломающий формулы ВПР."

      - id: "q3_copy"
        text: "Аналитик копирует данные из A1:B100 в ячейку D1. Как выглядит самый эффективный и быстрый с точки зрения производительности (обращений к ядру Excel) способ сделать это?"
        options:
          - id: "a"
            text: "Циклом на 100 строк, считывая GetValue и записывая SetValue для каждой ячейки."
          - id: "b"
            text: "Вызовом одной команды: sheet.GetRange('A1:B100').Copy(sheet.GetRange('D1'));"
          - id: "c"
            text: "Сохранить файл в CSV, обрезать в Блокноте и вставить обратно макросом ParseCSV."
          - id: "d"
            text: "Копирование в буфер обмена системы (Ctrl+C) через макрос SendKeys."
        correctOption: "b"
        explanation: "Метод Copy() отправляет ядру C++ Р7-Офис команду мгновенно скопировать блок данных во внутренней памяти (вместе с формулами и форматами). Это происходит за миллисекунды, тогда как цикл потребует 200 тяжелых обращений к документу."

      - id: "q4_vba"
        text: "В VBA вы объявляете переменные жестко: Dim ws As Worksheet. Как выглядит аналогичное объявление переменной для хранения объекта листа в современном JavaScript (Р7-Офис), где типизация динамическая?"
        options:
          - id: "a"
            text: "var = ws As ActiveSheet;"
          - id: "b"
            text: "let ws = Api.GetActiveSheet();"
          - id: "c"
            text: "Set Worksheet ws = Api.GetActiveSheet()"
          - id: "d"
            text: "Dim ws as let;"
        correctOption: "b"
        explanation: "В JavaScript для объявления переменных общего назначения (будь то текст, число или объект) используется современное ключевое слово let (или const). Слово Set, как и типы данных As Worksheet, здесь не применяются."

      - id: "q5_shift"
        text: "Метод Delete принимает строковый аргумент направления сдвига (например, 'up' или 'left'). Если вы выделили только ячейку C3 и вызвали: sheet.GetRange('C3').Delete('up'); Что сместится?"
        options:
          - id: "a"
            text: "Вся строка №4 поднимется вверх."
          - id: "b"
            text: "Вся строка №3 исчезнет."
          - id: "c"
            text: "Только данные столбца C, начиная со строки C4 и ниже, 'подползут' вверх на одну ячейку, а столбцы B и D останутся на месте (нарушится синхронизация строк)."
          - id: "d"
            text: "Таблица выдаст критическую ошибку, требуя выделить весь ряд (EntireRow)."
        correctOption: "c"
        explanation: "Delete('up') для одиночной ячейки (а не для массива строк) подтянет вверх только ячейки непосредственно ПОД удаляемой. Это часто приводит к фатальному 'разрыву' данных по строкам: имя Иванова в колонке B останется на месте, а его зарплата в колонке C уедет к Петрову (сдвиг)."
---

# Итоговый тест Модуля 3

Вот мы и закрыли один из важнейших этапов. В 3 модуле "Мастерство диапазонов" вы превратились из начинающего любителя в инженера алгоритмов Р7-Офис, способного удалять, перебрасывать и агрегировать гигантские пласты информации без единой капли "зависаний".

### Вы освоили:
- Внутреннее устройство API: Разницу между `Delete` (Сдвигом и разрушением) и `Clear` (Элегантной очисткой шаблонов).
- Фундаментальные основы алгоритмики (Использование Декрементирующих циклов "Reverse Loops" для удаления строк на лету).
- Введение в структуры данных JavaScript: массивы `[]` и метод `.push()` для поиска дубликатов. Золотое правило использования `String()` для приведения типов при сравнении.
- Математические трюки: Операторы деления `Math.floor` и остатка `%` для извлечения десятков и единиц. Вы написали ядро системы перевода "Сумма в пропись", которое до сих пор пугает бухгалтеров в стандартных функциях.
- Философию миграции с VBA на JS (отказ от `Set`, использование `let` и обязательные круглые скобки `()`).

У вас есть 7 минут (420 секунд) на 5 комплексных вопросов, моделирующих рабочие сценарии аналитика данных.

Проходной балл — 80%. Жмите "Начать тест" и докажите, что вы мастер диапазонов!
